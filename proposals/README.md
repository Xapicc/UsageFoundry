# Proposals

Design proposals that are not yet decisions. A directory here surveys one
question, gives every candidate its strongest honest case, and ends with a
single recommendation and the fact that would overturn it. Nothing in here
describes the code as it stands — `docs/agent/` is where the invariants that
have already been decided live, and `docs/` proper is what an operator reads.
A proposal is promoted by implementing it and moving its reasoning into those
two places; until then, treat every file here as an argument rather than a
record.

Every factual claim carries a `path/file.ts:42` reference or the command whose
output it quotes. A claim that could not be verified says "assumed" in the
sentence that makes it.

One directory is an exception to the first paragraph and says so at the top of
its own README: [CustomStacks](CustomStacks/README.md) was reopened on
2026-09-12 as a **build specification** after the operator decided the feature
is built, so its `01-constraints.md` is acceptance criteria rather than a survey
and no file in it may re-argue whether.

| Proposal | Question | State |
|---|---|---|
| [Sandboxing](Sandboxing/README.md) | What would full containment of a run look like — filesystem, network, and between two concurrent runs? | Open; one recommendation, unverified in its central mechanism |
| [ExternalValidator](ExternalValidator/README.md) | Should a finished run get a second, adversarial reading of task text against branch diff? | Open; the offline spike has run and is scored, and **nothing shipped** — no run is validated today |
| [ModelRouter](ModelRouter/README.md) | Who picks the model a run, a review or a delegated turn costs money at, and should anything but one global text box decide it? | Open; one recommendation, and it is **against building a router** — ship the per-run field and the read-back that already have wire support. Unverified: that a cheaper model does any of this install's real tasks in the same number of work cycles, which is the ~$10 experiment that would overturn it; and every reading of the `runs` table, which is unopenable from a work cycle and gates three of the ten options |
| [ContextControl](ContextControl/README.md) | What does a run carry from one work cycle to the next, what does carrying it cost, and can this app make it carry less? | Open; one recommendation, and it is **against building any of the thirteen mechanisms** — build the per-cycle composition readout, now including the compaction boundary, plus the two repairs the survey found while looking for something else. 82% of the bill is carried context, but at a tenth of the input rate, so that is the prompt cache's discount rather than waste; the one identified waste is 6.1% of the bill and its cause sits in the CLI's own system prompt. **Revised 2026-08-22** against context research in the operator's vault: the survey scored on cost, every published measurement since prices context compression as a *correctness* event, so an eleventh criterion at weight 4 re-scores the field — the recommendation holds and its lead widens from 3 points to 9, the runner-up changes from delegation to moving the volatile prefix, and app-driven compaction falls to last with three independent measurements landing on its exact mechanism. Two of the survey's own observations were refuted by probe and both in its favour: the CLI's compaction leaves a full marker in the transcript (twenty of them on this install, and nothing reads it), and a compaction **does** survive `--resume`. The provider's own `context_management` block is Option M and is rejected on **reachability** — five converging probes say no argv this app emits and no `--settings` key reaches it at CLI 2.1.226. Unverified: that a fresh conversation per cycle finishes the same task in the same number of work cycles, which is the single-digit-dollar experiment that would overturn it; whether a re-injected rule still *binds* after a compaction as opposed to merely being present, which nobody anywhere has measured and which sets the new criterion's weight; and whether the API's prefix match ends at a cache breakpoint, which is the difference between $1.44 and $5.02 a week for the one flag that reaches the mechanism |
| [GapRegister](GapRegister/README.md) | Where are this app's gaps — across frontend design, backend logic, growth limits, features it is missing, the app's own security and trust boundary, and what an operator can do when a live install breaks — and which of them are worth a survey of their own? | **Seventeen further rows closed whole on 2026-09-07, at `main` `a21beaa`, and the counts in this cell have not been re-derived since** — F5, B2/M4, B5, B6, F7, G1, G5, M6, M7, O2, O3, O6, O7, O8, S3, S4 and S5, off 52 commits written by parallel sessions that did not read each other and merged in one pass, with the row-by-row account in [05-register.md](GapRegister/05-register.md#what-was-implemented-on-2026-09-07-and-merged-onto-main-in-one-pass). **Thirty-one of thirty-seven rows are closed whole, one is half closed (S1), one is superseded rather than closed (F2, by that survey's own argument that closing it would imply a remedy nobody applied), and four are open: M2, O5, G3 and G4** — G4 measured rather than moved, its 20,000-row window priced at about eight months. **All three recommended surveys are answered**: reachability by [Findability](Findability/README.md), the landing boundary by being built and then finished (`resolveVerifyTools` became `resolveAllowedTools`, so the setting stops claiming to be a gate it is not), and the UI by [UIChecks](UIChecks/README.md) plus the smoke pass its Option C2 asked for — `npm run smoke-pages`, which is **red on one page of nineteen** and says so in a commit message and nowhere else. **The pass also produced the register's first evidence about itself**: the `db.ts` table claim M7 had just pinned went stale inside the merge, because the branch that dropped `plan_observations` for O6 never saw the branch that re-measured the claim, and `npm test` failed on it until the sentence was corrected. **Thirteen further rows moved on 2026-09-06, after everything in the cell below was written**, and the counts here have not been re-derived since: eleven closed whole (O1, O4, G2, M5, S2, B1, B3, F3, B4, M1, and G1's second instance of three) and two in half (B2/M4, S1) — commits `7d7e3f3`, `e355078`, `fd7c6dc`, with the row-by-row account in [05-register.md](GapRegister/05-register.md#what-was-implemented-on-2026-09-06-after-this-register-was-written). **F5, rank 1, is the one deliberately left**, on this directory's own argument that it is a survey rather than a patch, and that survey is now [UIChecks](UIChecks/README.md). The remaining halves are named rather than glossed: S1's revocation half cannot be closed without moving the gate off the edge runtime, and B2/M4's `resolveVerifyTools` still has one reader and it is still the conflict assist. **M1 closed by being done**: the first pull request this app has ever opened, `Xapicc/uf-deliver-smoke#1`, from one press against a scratch repository whose branch the app pushed itself — the account, and the three things it still does not establish, are in `docs/verification.md`. What follows is the cell as it stood at `66fdbab`. **Partially promoted; re-read row by row against `main` at `66fdbab` on 2026-09-06, and every count in the directory now describes the tree rather than the survey. Eight of the original twenty rows have moved — four closed whole, four in half — and twelve are open exactly as surveyed.** Four of the twenty rows shipped on branch `uf/usagefoundry-721638d11c0b-1-41e5e190`: **F1** whole (`/api/runs` reads `offset`/`limit`/`status`/`q`/`settledBefore` and the runs page pages the whole history — `7405720`, `d77638d`), **F2** in half (quick open asks the route for typed text so a run past the hundredth is reachable, but its corpus is still panes/runs/workflows, so a chat, branch, agent, template and schedule remain unfindable — `f7617fb`), **F4** whole (a text box and a kind picker over the events the page already holds, with the truncated-replay count stated when a filter is on — `e250524`, `e16fd7f`), and **F6** whole, plus the `beforeunload` prompt it had filed as an issue rather than a row (`a8f2984`, `bdbdf08`). Invariants moved to `docs/agent/conventions.md` and `docs/agent/testing.md`, the operator's half to `docs/runs.md` and `docs/install.md`, the click-list to `docs/verification.md`. Three more shipped later on `main` at `d1d3119`, out of PR #214 and the corrections that followed it, and **all three are mechanisms with no interface**: **B2/M4** in half (`landVerifyCommand` gates `landRun` and `deliverRun` against the run's own worktree — the merged version ran the check in the operator's checkout, standing on the target, where it could not see the work — but there is no Settings field and `resolveVerifyTools` still has one reader and is still the conflict assist), **M5** in half (`runs.priority` orders both promotion and the `queuePosition` readout through one comparator, after a second fix: priority reached the promotion order at the merge and not the number the page shows, so a run raised to the front started first while still reading as queued behind others — and no page has a control), and **M1** in half (`deliverRun` pushes and opens a pull request from one endpoint, never forced, never reached from the run loop, and its credential needed a fix because `git()` strips the whole `UF_` namespace — but there is no button and **no pull request has ever been opened by it**). An eighth closed later and without an implementation run pointed at it: **M3 whole** — `src/lib/notify.ts` POSTs one signed JSON body to one operator-named URL when a run ends `needs-review`, `blocked` or `failed` (`:107-111`, `:422`), success opt-in (`1891ad7`, `0d6af15`); it is whole rather than half despite having no Settings field, because there the absence is argued (`src/lib/config.ts:450-466` keeps a webhook target out of `settings.json`, since `/api/settings` is reachable with the master key). **F3, F5 and B1, B3, B4, B5, G1–G4, M2, M6 did not ship**, which is why the directory is not renamed — twelve of twenty rows stand as surveyed and four are half closed. Two open rows are contradicted in their evidence rather than closed: **G1**, since `/api/runs` was its named instance and now reads five parameters, leaving the pattern at two instances with `/api/chat` inheriting the description; and **B4**, whose "a turn has no cap" was wrong when it was written, because `--max-budget-usd` has been on the chat child's argv since before the survey (`src/lib/chat.ts:2327-2332`, `chatTurnBudgetUSD` = 2). One row got **wider**: **B1** has five unguarded doors beside `landRun` rather than four, because `deliverRun` (`src/lib/land.ts:2901`) came out of PR #214 resolving the same folder and taking neither check. **The recommendation was to survey reachability first, not to implement it, and the implementation was taken instead:** Survey 1's question — what should an operator be able to find, and by what mechanism, across chats, branches, agents, templates and schedules — is still unanswered, and the one-line falsifier (`SELECT COUNT(*) FROM runs` on the live install) is still unrun, so whether reachability was the right axis to lead with is no more established than before. Loop re-run on the branch head `a34e56b`: typecheck exit 0, `npm test` 1,660/245/0, `env -u __NEXT_PRIVATE_STANDALONE_CONFIG npm run build` exit 0. Re-run again for the `66fdbab` refresh, which changed no code: `NODE_ENV=development npm ci --include=dev` exit 0, `npm run typecheck` exit 0, `npm test` exit 0 at **2,259 tests / 351 suites / 0 failures**, `env -u __NEXT_PRIVATE_STANDALONE_CONFIG npm run build` exit 0, `npm audit` **`found 0 vulnerabilities`** — which closes one of the seven refutations by making it moot (`102050d` pinned `postcss` inside `next` rather than taking the semver-major move that argument declined). **No browser was opened and no container started for any of it, at any viewport**, and no GitHub issue was read, so every issue number in the register is assumed unchanged. What follows is the register as it was made, kept for the reasoning rather than edited into a record of what shipped: a register rather than a survey — **twenty verified gaps**, six candidates dropped for lack of evidence and six refuted as documented decisions, ranked by the cost of leaving them and discounted by confidence. The recommendation is **three surveys, eight issues and five refusals by name**, and the first survey is reachability: nine of the twenty rows are one sentence — the app cannot find what it has already done — and the repository already contains the fix twice, at `/api/branches` and at `/api/knowledge/search`, which is a real parameterised search pointed at the operator's vault rather than at their own runs. Second is the landing boundary, which carries the register's only row whose failure lands in the operator's product; at `66fdbab` half of it has been built rather than surveyed — `landVerifyCommand` gates Land (`src/lib/land.ts:999-1006`) and Deliver (`:2941-2947`) with **no Settings field**, the setting that sounds like it does still has one reader and it is still the conflict assist (`:1362`), the concurrency guard covers one exported door of **six**, and the push and the pull request exist (`src/lib/delivery.ts`) with no button. **A fifth axis, security, was added on 2026-09-06 and carries five more rows (S1–S5), ranked into the same table by the same method rather than appended; the register was twenty-five rows at that point.** That axis produced the register's **first violation of a documented invariant** — [S1](GapRegister/07-security.md), where `/api/logout`'s `all: true` branch is handed no cookie and revokes every session, against `docs/agent/security.md:26`'s *"it revokes the id inside the cookie it is handed"* — and its highest refutation rate: of seventeen candidates judged, six died against paragraphs that had already reasoned the thing through and six more were dropped for want of evidence. On the other four axes the original claim still holds: every row there is something `docs/agent/` never had an opinion about. **A sixth axis, operations and recovery, was added the same day and carries eight more rows (O1–O8), ranked in by the same method; the register is thirty-three rows now.** It asks what an operator can find out when a live install breaks, what they can get back and what is silently unrecoverable, and its result is that the recovery *mechanisms* here are the best-argued code in the repository: seven of its eight rows are a mechanism that exists and **cannot be seen** — `lastBootReconcile.closed` never clears while the outstanding count sits on a route the read-only status token cannot reach (`src/lib/status.ts:315-321`, `src/middleware.ts:114`, `README.md:250`); every fault `migrate()` finds in the database it just opened is a `console.error` and `PRAGMA user_version` reaches no route or page; nothing under `src/` knows the backup directory exists, so no surface can say when the install was last backed up; `stop_grace_period: 30s` and `SHUTDOWN_GRACE_MS = 10_000` are one edit apart and `deployment.test.ts` pins every other such pair and not that one; a chat thread and every message in it is permanent with no horizon and no delete handler anywhere. The eighth is the register's **second** violation of a documented invariant — `lockVerdict` asks staleness second where `docs/agent/concurrency-and-ownership.md:18` says it asks it last — which costs nothing, because there the code is safer than the sentence. **It is also the axis surveyed with the least access**: no Docker, no readable `DATA_DIR` or `/backups`, no server started, so every claim about a running container is a reading of the file that configures it, the half of backup and restore that was checked is the code and `docs/verification.md:409-437`'s record rather than the packaging, and eleven candidates were dropped for want of evidence against eight filed — the only drop list in the directory longer than its own row list. Loop re-run unchanged: `npm run typecheck` exit 0, `npm test` exit 0 at 2,259/351/0. Unverified: every reading of run history, because `DATA_DIR` is unreadable from a work cycle and the in-checkout database is stale — `SELECT COUNT(*) FROM runs` is the one-line falsifier that would reorder the top two; and whether any landing collision has ever occurred, which needs a running container. **A fourth pass on 2026-09-06 added no seventh axis and four rows to the axes that already existed, taking the register to thirty-seven:** **B6** (`orchestrator.ts`'s Claude stream parser drops an unrecognised event type in silence while the Codex parser twelve lines below it logs one and its docblock names the Claude parser while arguing why — `src/lib/orchestrator.ts:7372-7381` against `:7511-7523`), **F7** (the workflow editor holds the whole graph in React state until Save and warns about nothing — `grep -ran "beforeunload" src/` returns `src/app/settings/page.tsx` alone), **G5** (the chat route re-reads, re-sorts and re-serialises every message in the thread on a three-second poll, with no cursor, over a table on no horizon — the register's first row argued from an `EXPLAIN QUERY PLAN`, taken against a schema `migrate()` built in a scratch `DATA_DIR`), and **M7** (nothing in CI, `npm test` or a linter reads a doc, and two `docs/agent/` completeness claims that were filed and closed as #161 and #166 are wrong again — 22 tables claimed against 34 created, thirty-odd `globalThis` keys claimed against 58). It also widened **G1** with a fourth capped list — a workflow's instance history, the newest twenty on a route that reads no parameters — and refuted seven candidates, five of them by measurement rather than by a paragraph, including that the per-run `globalThis` maps leak and that any hot query on the `runs` table misses an index. **It is the first pass to read GitHub**: `gh issue list --state closed --limit 300` returned 181 titles, none of which is any of the nine numbers the register cites, so all nine are confirmed still open — no body was fetched and nothing was opened, closed or commented on. Loop re-run unchanged: `NODE_ENV=development npm ci --include=dev` exit 0, `npm run typecheck` exit 0, `npm test` exit 0 at 2,259/351/0. Still no browser and still no container. It stopped partway through its own brief and says where: `workflows.ts` was mapped and read at one section, and `orchestrator.ts:1137-1979` — the refusal-classification band — is what it names as the next thing to read |
| [OperatorInterface](OperatorInterface/README.md) | Sixteen pages and roughly fifty components are read by one operator supervising money-spending agents. Where does this interface fail a reader who is not the person who built it, and what is worth changing? | Open; one recommendation, and it is **six hex values, a dozen class strings and one test file**: repair four measured contrast failures and then pin them, in that order, because a floor asserted over today's tokens pins the failures instead. The four: the focus ring is `--accent` at 45% alpha and measures 1.93:1 to 2.26:1 against its own surround on every focusable element (1.4.11 asks 3:1; 75% is the first round figure that clears it everywhere); `--fg-faint` is 3.19:1 to 3.62:1 on every light surface at 11 to 13px, and reaches 192 rendered strings through `ListRow`'s description, `Hint` and `Empty`, which is where `docs/agent/ui-density-audit.md` deliberately *moved* facts a reader needs; white on `--danger` is 2.82:1 in dark on every irreversible button, where `globals.css:76-80` documents inventing `--tint`/`--tint-fg` to solve the same failure for blue and measures 5.06:1 for it; and `--fg-muted` is 3.54:1 on the bezel, which is the `/settings` section chips. `--fg-faint` has **no** repair as a token (the lightest value clearing 4.5:1 on all four light surfaces is `#6d6d71`, against `--fg-muted`'s `#68686d`), so the palette holds two text weights at AA in light mode, not three, and the fix is at three kit call sites. **Refused by name:** axe-core in CI (jsdom cannot resolve `light-dark()` inside `color-mix()`, so it reports `incomplete` on all four findings, and the affordable configuration is a rendering harness wearing an accessibility argument), raising the type scale (`globals.css:707-711` already refused it in writing, and browser zoom is a better instrument for an angular criterion than a token), and a progressive-disclosure restructure of `/settings` (two named prohibitions in the density audit, plus SearchPilot 2020 at +12% and Peytchev et al. 2010 at 36-45% to 60.7% both pointing the other way; the real gap is a field search, which is GapRegister F6). The keyboard pass **dissolves**: roving tabindex, `tabIndex={0}` on scroll containers, a native `<dialog>`, and the only thing wrong with it is a colour. Unverified: **no browser was driven at any viewport and no screen reader was used**, so nothing here is a judgement about how anything looks; the falsifier is one reading of the ring at 75%, which turns a halo into a nearly solid outline in a palette explicitly imitating AppKit; and the document-outline finding (eleven peer `<h2>`s on `/`, three of which are the cost-source containers) is filed as a question rather than a change because the instrument that would settle it is the one nobody here has |
| [UIChecks](UIChecks/README.md) | 40,572 lines of page and component code are rendered by **zero** tests. What should check them, and — the half that is easy to skip — what would each candidate actually catch? | Open; *Survey 3* of GapRegister's recommendation, which ranks its row **first overall** and asks for a survey precisely because "add Playwright" is the answer to be suspicious of. Five options, **two recommended and three refused by name**. The recommendation is a **floor rather than a strategy**: a four-item written pass in `docs/verification.md`, then a **smoke pass** — start the built app against a throwaway `DATA_DIR` and a `CLAUDE_BIN` that cannot spawn, open all 19 pages at two widths, assert 200, no console error and no horizontal body scroll — then more `renderToStaticMarkup` files at whatever rate is convenient. The argument that does the sorting is a partition of the app's claims into five classes by *the cheapest instrument that decides each*, and it disposes of two options on structure rather than on taste: **`renderToStaticMarkup` cannot reach layout** (one pass, no state, no effects, no CSS), and **jsdom implements no layout at all** — zero box model, `getComputedStyle` returns declared rather than resolved values — so a `@testing-library` suite reaches state machines while reading, from inside a test file, as though it covered the page. That over-reading is why it is refused and it is the closest call here. Measured: **20,765** lines across 19 pages, **19,807** across 63 components, **2,289** tests of which **11 files** render anything and **0** render a page, **222** viewport-conditional class occurrences (93 distinct, 39 files) that nothing evaluates, **33** entries on the *not yet verified* list, and page code up **26%** since the row was written with rendered-page coverage at zero throughout. **Refused by name:** a full Playwright suite (the whole measured maintenance tax — 44.7% of UI flakiness is the async-wait race, **31.1% of flaky UI tests are repaired by deleting the test** — for a benefit no published study has ever measured), visual diffing (every page renders live data, so a baseline fails for reasons that are not defects, and no measurement exists of what it catches that an assertion does not), and jsdom as above. It does **not** re-open `proposals/OperatorInterface/`: contrast is settled there with arithmetic on declared tokens, which is a *better* instrument than a scanner because `light-dark()` inside `color-mix()` is exactly what a non-rendering engine returns `incomplete` for; that survey's conditional refusal of `axe-core` is resolved here and left standing on its second ground. **The uncomfortable finding, and it is the reason this is a floor:** nobody has ever measured what a browser test detects that a unit test does not — two papers by the same group a decade apart price browser testing on development and maintenance cost and never on defects found — so the case for reaching layout at all rests on **one recorded defect**, the `w-auto`/`w-full` ordering in `RunLand.tsx`, and is stated as a sample of one. **Unverified: no browser was opened, no harness was built, and the smoke pass has never been run** — how many of the 19 pages fail it the first time is both the falsifier at the top of the recommendation and the first run of the thing recommended |
| [Findability](Findability/README.md) | What should an operator be able to find in this app, and by what mechanism, across chats, branches, agents, templates and schedules? | Open; **Survey 1** of GapRegister's recommendation, the one that directory ranked first and that four point fixes were taken instead of. Measured against `594ac64`: **68 API routes, five of which read a `q=`** (`runs`, `chat` and the three `knowledge` routes), **four text inputs in the whole UI**, and quick open's corpus is **three hardcoded blocks** at `src/components/shell/QuickOpen.tsx:203-252`, so no route and no lib function searches more than one kind at once. The recommendation is that **findability is a property of a route, not of a page and not of an index**, three things ranked: give `q=` to the two corpora measured large and lacking it — `/api/branches` (141 branches on this machine, 143 fifteen minutes later) and `/api/dreaming` — using the predicate `orchestrator.ts:1044-1049` already runs; **derive** the destination list instead of hand-registering it, because `PANES` has already drifted; and **decide out loud that agents, templates and schedules are not search targets**, since their lists are already complete and a schedule is a `UNIQUE` column on a workflow. **Refused by name:** a cross-kind index, a query language, and a generic per-page filter component; widening quick open is refused *as a shape* and accepted only as a consumer of the first item. **GapRegister F2 is superseded rather than closed** — four of the five kinds it named do not survive measurement, and closing it would imply a remedy nobody applied. **No product code changed**, and three defects were filed without being ranked into the register: `/runs/[id]/conflicts` is in neither `PANES` nor `toolbarTitle`, quick open's run items carry a `haystack` nothing reads, and `request_log` is written by 44 route files and read by nothing. Unverified: **no populated install database exists in this container** — the two found answer `0` to `SELECT count(*) FROM runs`, every content table empty — and Docker is unavailable, so the falsifier at §10 is still the command a human has to run |
| [UnattendedOperation](<implemented - UnattendedOperation/README.md>) | This app's premise is that a run works while nobody watches. Nothing it produces reaches an operator who is not looking at the page. What should, by what route, and what does each route cost — in money, in code, and in what leaves this machine? | **Promoted**; steps 1, 2 and 4a shipped — `logLifecycle` routes `needs-review`, `blocked` and `failed` at `warn`, `run.error` carries the 429 ladder's `retrying` and `usage_limit`, and the outbound webhook posts six signed fields per ending that wants a person, records every attempt in a bounded table and reports the count since the last success on `/api/status` — with the invariants moved to `docs/agent/run-lifecycle.md` and the operator's half to `docs/install.md`. **Step 4b, the in-app digest, was deliberately not built**, and it is still the better second build for the comparison's own reason: the webhook subscribes, so the ending it cannot reach is the one that leaves no event, and the dead container login is still readable only by looking. All five refusals below stand. What follows is the argument as it was made, kept for the reasoning rather than edited into a record of what shipped: nine endings and waits enumerated from the code — `needs-review`, a refusal park, the 429 ladder, a guard trip, a blocked dependency chain, a merge queue on a dirty checkout, a halted workflow instance, a dead container login, and a boot reconcile that closed runs — and **none of the nine then had any route to a person other than the operator opening a page**. The recommendation is **four steps and it does not start with a channel**: level `logLifecycle`'s `status` case, which is one `info` line for all nine statuses so `needs-review`, `blocked`, a park and an ordinary `completed` are indistinguishable in level to any log shipper (and `grep -c "needs-review" README.md` returned **0**, so the one ending whose entire content is "a person should look at this" was unnamed in the document that tells an operator what to watch); project the `retrying` and `usageLimit` booleans the 429 ladder already emits per rung and `logLifecycle:566-568` drops, which is two fields on an existing call and the best ratio in the survey, because that ladder holds a folder, a worktree slot and one of `maxConcurrentRuns` for ~17-26 minutes while reading `running` in every state-based view; then **ask the operator one question** — do you have, or will you run, an HTTP POST receiver — and build either the outbound webhook (six signed fields beside `logLifecycle`, `fetch` is global so the dependency count stays at four, chat channels documented as receivers so the app never holds a vendor's token) or the in-app digest (an SSE consumer of the `bus.emit("*")` at `orchestrator.ts:513` that already fires after the durable write and has **no reader anywhere in the tree**). The spine of the comparison is that **the two interrupt rows need opposite mechanisms**: the 429 ladder leaves no state and is only reachable by subscribing, the dead login leaves no event and is only reachable by reading, so nothing scores full marks on the highest-weighted criterion. **Refused by name:** web push (the endpoint is chosen by the browser vendor, reaching a non-host device means exposing the app past `docker-compose.yml:62`'s loopback bind, and RFC 8291 is 300-500 lines of hand-written crypto whose every bug returns 201 and delivers nothing), email as a default (the only channel with no zero-dependency route, and the only one whose usefulness is proportional to its leak — an interrupt can be content-free, a digest cannot, so a useful digest email is the install's work log in plaintext), an MCP tool as scoped (forty lines of tool and a fifth credential shape on the one route `middleware.ts` exempts), the orchestrator chat (paying a model to proxy a `SELECT`), and a chat bot that can act (`docs/agent/run-lifecycle.md:48` forbids a bulk control from answering `needs-review`, and the app has already decided that a decision is taken where the operator can see what they are deciding about). The brief's premise about the PWA was checked rather than trusted and is **half wrong**: `src/app/manifest.ts` is a real installable manifest; only the service worker is absent. Unverified: **every** frequency — `ls -la /data` is `Permission denied`, the stale in-checkout database has zero `runs` rows and one `ops_events` row, so no park, ending mix, queue depth or dwell time exists anywhere readable and every latency claim here is a mechanism claim; and nothing was ever delivered — no provider contacted, no webhook fired, no egress from this container tested |
| [GrowthLimits](GrowthLimits/README.md) | What breaks first as this install grows — more concurrent runs, more repositories, more months of history — and which ceiling is worth raising before it is hit? | Open; **thirty-one bounds catalogued with file, line and what happens at each one, and the answer to the first half of the question is "nothing"**: zero of the thirty-one fail, throw, corrupt or lose data, and zero numeric bounds are reachable within thirty days at the measured growth rate of ≈88 transcript files and ≈69.6 MB per day. So the recommendation is **against raising any ceiling** — instrument two axes and take one reading. Report the cold transcript scan's duration on `transcriptCacheStats()` and `/api/status` (measured cold **2,985-3,041 ms** against warm **82.5-88.9 ms**, a factor of 36, paid on the first `/api/usage`, `/api/status`, pre-cycle guard or `assistRefusal()` after every restart because the cache is process-local), with no threshold and no warning colour because a 6-second cold scan is a fact about a corpus rather than a fault; then make `walkRepo` return `{files, partial}` so the file price list confesses a short walk, which matters not because the bound is near — measured occupancy is 9.8% and 6.2% on the two readable repositories — but because it is one of only **two** silent truncations in the survey, its reader is an agent frozen with the notice for the life of the run, and the identically-named, identically-justified `MAX_WALK_ENTRIES` in `retention.ts` already returns `partial` and its docblock argues that a bounded walk must say when it stopped. The reason nothing breaks is **the retention defaults**: `eventRetentionDays: 30`, `checkoutRetentionDays: 7` and `transcriptRetentionDays: 30` make three of the four history-growing costs plateau, at ≈2,650 files, ≈2.1 GB, a ≈6.4 s cold scan and cache occupancy of ≈218,000 of 500,000, so `TRANSCRIPT_CACHE_MAX_ENTRIES` is unreachable under the shipped default. Four bounds carry a docblock naming a growth problem in the past tense — **the app is not approaching its ceilings, it has been walking away from them** — and the two costs that would have multiplied with `maxConcurrentRuns` when #68 was written now multiply with neither. `maxConcurrentRuns: 4` is the only non-refused raise and is **deferred on one `docker stats` reading**: `settings.ts:765` says having a ceiling was "Measured, not reasoned" and `settings.ts:205-232` says where it sits rests on ≈1.5 GiB per work cycle, which "is reasoned rather than measured"; near 1.5 GiB and 4 is right, near 600 MB and two thirds of a 10 GiB budget is unused. **Refused by name:** raising the four-mount ceiling (there is no cap in `src/` to raise, `parseMounts` is forbidden from gaining one, and #77's silent fifth slot now refuses the boot, is documented at `docs/install.md:493-505` and unit-tested), hardening the SQLite write path (`SQLITE_BUSY` needs a second writing connection the server lock exists to prevent, and checkpoint starvation needs a long-lived reader — `grep -rc "\.iterate(" src/` sums to **0**, which closes the class more firmly than a load test), and putting `request_log` on a time horizon (rows were chosen over an age deliberately, and the cap is a term in the security argument that keeps `/api/mcp`'s 401 outside `auditMutation`). Seven open issues reconciled with a verdict each and **nothing filed, closed or commented on**; ten distinct sub-issues across them no longer reproduce at HEAD. Two findings correct a neighbour rather than restating it: peak RSS is **323-372 MB** at 973.8 MB of transcripts, not the 1:1 #68 measured, because `SCAN_CONCURRENCY = 12` bounds the fan-out; and a polling browser writes **no** audit rows at all, because `auditMutation` wraps 33 exports plus `/api/mcp`'s POST and **zero** GET handlers, so GapRegister G4's rate driver is agent traffic rather than page loads. Unverified: **every concurrency figure** — Docker is unavailable here, no load test was run, no second run was observed, and the per-child memory number the whole axis rests on cannot be taken from inside the process; **all run history** — `/data` is `Permission denied` and the stale in-checkout copy holds 0 `runs`, 0 `run_events`, 8 `request_log` and 1 `ops_events` row, so every growth rate here comes from the transcript corpus as a proxy; `readCountsFor`'s `json_extract` at a real row count, where 8.29 ms against an empty table is a floor and the project's own 38 ms against 131,572 rows implies ≈950 ms of blocked event loop at `MAX_WORKFLOW_NODES = 25`, the largest synchronous cost found; and **anything about more than two repositories**, n = 2 mounts of one repository each, two orders of magnitude below `MAX_FOLDERS_PER_MOUNT = 400` |
| [ContinuousImprovement](ContinuousImprovement/README.md) | How does an install stop its runs re-deriving what earlier runs already established — the same mistakes and the same reading — and what does each way of doing it cost? | Open; one recommendation, and it is **against building a memory** — build the cross-run readout and the contention card, ship the prior-read pointer behind a 50% holdout in the same change, and refuse five of the eleven options by name. 73.2% of `Read` calls are of a path an earlier run on the same repository already read, but 50.6% of those are files the run then edits, so the addressable share is 36.1%; the repeated-*mistake* corpus is two ending-level failures in 294 runs. The survey's own control measured a generated in-prompt notice reproducing the exact command it names in 56 of 66 runs against 7 of a matched control of 175 — a 21x lift on the command form against only 1.1x on the behaviour — where CLAUDE.md's gate, in the same message, is declined by 101 of 112, so what is complied with is specificity rather than position. Unverified: `d`, the share of aimed-at reading a pointer actually removes rather than reorders, which nothing in this repository measures and which every saving claim multiplies by; and whether the gate decline is about position or content, which is the fortnight-long holdout that decides the runner-up |
| [Dreaming](Dreaming/README.md) | Once a day, something reads that day's sessions and writes what was learned into the operator's vault. Should this install build that, in what form, what does each form cost, and what does it collide with? | Open; one recommendation, and it is **against building the feature as posed** — ship the recurrence readout (no model, no write, no clock), run a fortnight of the composed-workflow experiment, and only then put **one question** behind a person's press. Four of the seven options are refused by one fact the survey did not have to reason its way to: **the destination has already published a rule for exactly this session.** `/workspace2/AGENTS.md:115` — "If you are a session from another project and have not read `CLAUDE.md`, you do not have the writing conventions and **should not write notes here**. The one exception is a single question capture into `3 Resources/Questions/Inbox/` … a quarantine that gets reviewed before anything counts as vault content" — and a UsageFoundry-spawned child is that session. Beside it, `src/lib/knowledge.ts:39` refuses the write from this end for a concurrency reason rather than a preference, `_Meta/qc.py` fails the operator's **whole vault** on a malformed note, and **there is no `.git` in the vault**, so retraction is a person deleting a file if they notice. **The ContinuousImprovement Option G refusal carries on authorship and not on cost, and the survey argues that rather than borrowing it**: reading a day's prose once is **$2.57 against a measured $956.09 day (0.27%)**, $18 a week, and reading only the day's tool errors is **$0.06 a night** — two orders of magnitude below the 12.4–27.6% that closed Option G, which fired 294 times where this fires once. What kills it instead is that **the corpus supports transcription and not diagnosis**: 48,978 `thinking` blocks with **thirteen non-empty**, none from the model this install runs (replicating RunDecisionTree on a corpus 1.7× larger), `src/lib/orchestrator.ts:6675`–`:6704` dropping every non-`text`/`tool_use` block by name, and the operator's own vault reporting the best method in the only peer-reviewed benchmark locating a failing step **14.2%** of the time, with the working position "admit transcription, mark diagnosis as a hypothesis, and never let an unverified stated cause enter a store as a fact". Retirement is scored as a first-class criterion and is where every written option fails: shipped memory systems notice their own memories have been invalidated **5.1%–17.8%** of the time, **retrieval selects rather than averages** (sub-0.1% poison → >80% attack success, peer-reviewed), and the reader folds to a stated position 32%–98% of the time — so a wrong nightly note is not diluted by right ones, and the consequence does not scale with the rate. **Deduplication is measured rather than reasoned about**: 2,548 tool errors in 24 days, 1,175 signatures, **13.5% of a night's distinct signatures already seen on an earlier night, 30.3% of instances, 49.5% of all instances in a multi-day signature**, climbing to 36% and 57% on the last two days with material. "Every session" turns out to be three corpora that disagree — on 2026-08-28, **63 sessions: 42 run worktrees, 11 container checkouts, 10 on the operator's own machine** — so a Dreaming on `runs` sees 42 of 63 and 74.6% of the money while the operator's own sessions are the largest prose class in the install. And the loop it would close **has already run three times by hand**: all three notes in the vault's quarantine were captured by external sessions, two naming UsageFoundry, one of them the source of this survey's evidence — and none has been triaged since 2026-08-15. Two things the survey got wrong and corrected against the tree: **the transcript corpus is readable from inside a run** (the containment pair fixes a run's cwd, not what it may `Read`; the managed sandbox's whole filesystem clause is `denyRead: ["/data", "/backups"]`), and `describeEvent`'s latent defect is at `logLine.ts:561`, not `:480` — all three of the `review.ts` defects still reproduce, one now contradicted by its own docblock. One repair found on the way: **`searchKnowledge` (`knowledge.ts:1403`–`:1438`) reads neither `confidence:` nor `status:`**, so a `seed`/`low` note ranks beside an `evergreen`/`high` one and the vault's authority gradient is invisible to the app that reads it. Unverified: **every reading of run history** — `runs`, `run_events` and `run_reviews` are all 0 rows in the only openable database, `/data` is empty and the neighbouring copies will not open, which leaves Option B scored on the shape of its types and not on a count; whether `qc.py` would actually reject a nightly note, read from its check table and never run; and **whether any of this helps at all**, which nobody anywhere has measured — the literature bounds a correctness effect at "10 to 15 points and probably zero" at a power none of the studies reached, and the operator's own vault holds the question open at `confidence: low`, in the quarantine, with UsageFoundry named as what prompted it |
| [RunDecisionTree](RunDecisionTree/README.md) | An operator opens a finished run and can see the log tail, the report and the diff. Where did it branch, what did it choose, what did it discard, and why — and where can any of that be read from? | Open; one recommendation, and it is **build the smallest option first and defer the one the brief asks for**: ship the `run_events` timeline (1-2 days), fold the transcript's structure into it (2-3 more), run the first-hand decision log as a ten-run experiment, and do not build the model reconstruction yet. The survey turns on one re-measurement: **the model's reasoning is not in the transcript and for the model this app runs it never is** — `claude-opus-5` produced **28,857 thinking blocks with zero non-empty bytes** across 266,362 records, every record carrying an `effort` field is empty 28,859 times without exception, and the only 11 non-empty blocks in the corpus are Haiku 4.5 probe sessions replying `"ok"` (3,905 bytes total). That strengthens ContextControl's finding rather than repeating it, on a corpus that has since roughly doubled, and it adds the distinction that pass did not draw: the strip tracks the `effort` field, not the model. Three further measurements narrow the design — the grounding run wrote **5,578 bytes of assistant prose across 297 tool calls** (eleven stage directions totalling 680 bytes, one sentence that states a reason, one 4,873-byte report) and made **zero** `TodoWrite` calls; `isSidechain: true` appears **0 times in 266,362 records**, so a delegated decision leaves one call and one result and, past ~50 KB, a `<persisted-output>` pointer to a file the retention sweep does not know about; and **`run_events` already knows more than the transcript does** — its `kind` union (`src/lib/apiTypes.ts:1711`) separates `assistant` from `tool` from `tool_error` from **`subagent`** from `iteration`, is written from stdout as the run happens, survives `expiredTranscripts` because it cascades on `runs`, and is keyed on `run_id` so a resume needs no handling. That is why the cheapest option wins the weighted score (E 153, D 146, B 132, A 118, C 118, recomputable via `node scripts/score.mjs`) and why Option A alone is defensible in no configuration tried. **Cost is not why the reconstruction lost**: the decision-bearing slice of a transcript is **3.6%** of it, so a full Opus 5 pass costs **$0.42 against a run that cost $43.51** — 0.96%, or $0.08 on Haiku 4.5. It lost on trust — the grounding run's five tool failures are all shell typos, and every one presents to a reconstruction as "tried X, found it inadequate, narrowed to Y", which is five ready-made fabrications in one run — and on having very little to be right about, since it reads the same 13 sentences the free options read. **Refused by name:** causal edges from any option but the declared log (nothing licenses "because"), a `rejected` column before a run writes one (a path not taken leaves no trace), a new layout algorithm (`autoLayout` at `src/lib/canvasGraph.ts:149` takes `{id}[]` and `{from,to}[]`, which is a decision tree), and a canvas in phase 1. The parent chain **does** break — `parentUuid` is null at all four of the grounding run's compaction boundaries — and `logicalParentUuid` repairs it 4/4, with `preservedMessages.uuids` naming the five records that crossed, which is the difference between a tree that fragments into five components and one that says "the run below this line could not see the 22 files it read above it". Unverified: whether the appended prompt reaches a sub-agent, which is one of the declared log's two best properties; whether a commit message survives into a `run_events` payload, which decides whether the timeline keeps the densest first-hand `why` in a run; and whether the provenance rendering actually stops an operator over-trusting an inferred sentence, which is a five-minute exercise that gates eight days. **The two runs the brief named could not be used** — both databases in this image hold zero `runs` rows and neither id resolves to a transcript — so the grounding run is a real UsageFoundry-spawned agent run against this repository's own worktree lineage, and two of this proposal's own figures were caught wrong by its validation pass and corrected |
| [KnowledgeSection](KnowledgeSection/README.md) | The operator says `/knowledge` is overwhelming, its node view is hard to navigate, and it is not visible at first look what they are looking at. What is worth changing, with the graph as the priority? | Open; one recommendation, **built as a sequence of three commits rather than one change** — move the graph above the Notes table (two lines), give the canvas an orientation layer, then fold the panel. The survey turns on one dated fact: **`/knowledge` landed 2026-08-21, two days after `docs/agent/ui-density-audit.md` landed on 2026-08-19, and was never one of its eleven surfaces** — so §1.0's Delete/Group/Reorder/Hide has never been run here, which is exactly why `OperatorInterface/09-option-h`'s refusal of progressive disclosure on `/settings` does not carry, and each of that refusal's other three grounds is answered rather than ignored. Two corrections to the brief, both making the problem different rather than smaller: **the open-note marker does exist** (`KnowledgeGraphCanvas.tsx:303-310` rings it) and is drawn in `--tint`, which is the *same hex as `--accent`* in light mode — the colour a hovered node is filled with — so the "you are reading this" mark and the "your cursor is here" mark collide, a defect `GROUP_PALETTE`'s own docblock shows the repository already reasoned about and fixed for the seven group colours; and **the panel is 64 focusable controls on a first visit, not the ~25 counted**, because the seed writes `MAX_GROUPS` = 7 colour groups at five controls each, so 48 of the 64 are the one block used once. Three further measurements shape it: the browse table is **50 rows** and `ListView`'s 320px cap is *deliberately released* below `md`, putting roughly 250 stacked lines between a phone reader and the graph, which one JSX move deletes; a node's size is its degree **in the drawn slice** (`countDegrees(sim.nodes, sim.edges)`) while the wire carries the vault's, so a filter shrinks a node without the vault changing and nothing says so; and `fitView` is written, called once, and unreachable after a single pan. No single option scores above 1 on more than one of the operator's three sentences, which is the whole argument for a combination: an orientation layer alone answers "not visible" by making "overwhelming" worse. **Refused by name:** splitting the route (D — E1 gets most of it for two lines, and its own cost is a 7.3 MB fetch per tab visit), four tabs specifically (a note tab and a Notes tab break the "why the note leads the page" decision), a keyboard route into the graph (`KnowledgeGraphCanvas.tsx:70-75` decides against it with a reachable named alternative), a tooltip or hover-only readout, a `Sheet` for the colour editor, joining the graph's search to the list's filters (refused twice in the tree, the second time noting it was got wrong once), an eighth `globals.css` token, `useImperativeHandle` (zero hits in the repository), and making `textFade` relative to the fitted zoom. Unverified: **the zoom `fitView` produces on the real vault**, which decides whether `textFade: 1.1` means the first paint is unlabelled — if the graph fits above 1.1 the survey's cheapest answer to the third complaint is wrong outright; whether a 1.2px stroke is visible on a 2.5px-radius attachment; whether `aria-live` on the readout helps or floods, which needs a screen reader; and **whether the operator wants the graph as a destination**, one sentence that moves the refused option from sixth to first. Nothing under `src/` changed; `npm run typecheck` exit 0 |
| [ProposalBoundary](ProposalBoundary/README.md) | Does an approved proposal become the run the card described, every time — and when it does not, is the operator told? | Open; a survey rather than an option set, and its answer is **no, in six ways, five of them reproduced end to end** by the scripts it ships. Ranked by what they cost: **a cancelled or timed-out turn's child settles the *next* turn's row** (`chat.ts:2340` latches on `status='thinking'` and carries no turn identity, while `endTurn` returns without waiting and `claimTurn` admits any non-`thinking` row) — measured, the operator's new message is answered by the turn they stopped, their real answer and its cost are discarded, and a third message is then accepted with two billed children live; **a refusal that clears on its own destroys the proposal permanently** (`approveProposal`'s catch marks `failed`, which is terminal, and its own comment justifies that by listing only the *permanent* refusals — the install ceiling and the data-dir lock are neither, and in a batch one ceiling trip destroys every member); **the guard set applied at approval is re-read at the click and nothing pins it to the card** — measured, card said `plan · own checkout · 3 cycles · $5.00`, run started `bypassPermissions`, in the operator's own folder, uncapped in cycles and money, which is precisely what `docs/agent/chat.md` says the gate exists to prevent; **"only one proposal may continue a run" is caught only at `admitDependencies`** and catching it burns the proposal, with a sentence naming two run ids that were on no card; and **a restart leaves no record in the conversation** and `claimTurn`'s `error=NULL` erases the one it left on the row. Four are **repairs** — each has the code's own comment as the witness — and one, the guard drift, is a **design change** and is recommended for a decision rather than a patch. Seven things were checked and **hold**: deleted template and deleted agent both refused by name, an agent that sets no guard, a dangling dependency id caught at proposal time, a cycle unconstructible at proposal time, `chatOwnsRun` that cannot be made to over-grant, `promptOverride` reaching the agent in order and untruncated, and pending proposals and open questions surviving a restart actionable. **Coverage gap, itself a finding:** every pure function on the approval path is tested and nothing that writes is — `finishTurn`, `claimTurn`, `endTurn`, `approveRunBatch`, `markProposal` and `reconcileChatsOnBoot` have no test between them, and four of the six findings live in exactly those. Unverified: whether a signalled `claude` prints its final JSON, which sets the size of the money leak beside the first finding but not whether it happens; whether an answer racing an ordinary message loses its record on the row; whether a reused `spec_id` shadowing an approved one is a defect or the safe answer; and how stale F3's card really gets in a backgrounded tab. Nothing under `src/` changed; `NODE_ENV=development npm ci --include=dev`, `npm run typecheck` and `npm test` (2,085 pass, 0 fail) all exit 0; Docker unavailable, so no compose smoke test |
| [ChatPanelExperience](ChatPanelExperience/README.md) | A turn in the orchestrator chat spawns a child agent and is allowed ten minutes. What does an operator know, and what can they do, during and after those ten minutes? | Open; one recommendation, **the cheap tier as four commits** — about 100 lines, no migration, no new route, nothing on the approval or spawn path, closing twelve of the twenty findings in full and three more in part. The single change if only one is made: carry `turn_started_at` on the DTO and name the ten-minute deadline, ~15 lines, because the elapsed clock is measured from the last thread message (`page.tsx:706`) while the server holds the real start (`chat.ts:1617-1621`) and a mid-turn `save_template` note (`api/mcp/route.ts:1635`) resets it to zero. Three headline findings, all with the browser measurement behind them: **a mid-turn activity feed is not "add a stream"** — the chat's child runs `--output-format json` and buffers stdout to exit (`chat.ts:2016`, `:2122-2126`, `:2153`), where a run gets `stream-json`, a `tool`/`tool_error`/`subagent` parser and an SSE route with replay, so every piece exists and what is missing is a table, a migration, a retention decision and a rewrite of `parseTurnOutput`; **the one ending with no permanent record is the only one drawn in red** — four of five endings append a system message and `reconcileChatsOnBoot` writes the row alone (`chat.ts:2446-2453`), while `claimTurn` clears `error` on the next message so recovering erases the evidence, and a ten-minute timeout is drawn in the same grey as "The chat saved a new template"; and **the approval gate's reasoning and its geometry disagree** — with 26 pending at 1440×900 the list shows **1.8 cards of 26** (317px of 3791px) under a button reading `Approve 26` and the sentence "under the guards shown on each", where each card clips the task to a third of itself (162px of text in a 54px box, no `title`, no expander) and a templated card's guard set is the template's *name*, `Bug fix`, against real guards of `acceptEdits · own checkout · 12 cycles · 45 min · $4.00`. Two corrections to the brief, both narrowing it: the panel is **not** silent during a turn (proposals and questions are rows and the 3s poll reads them, so a proposing turn has a pulse — a *reading* turn, which is what the system prompt asks for, has none), and the operator's message is **never** lost (appended before the spawn, `chat.ts:1848`) though nothing says so and there is no retry. **Refused by name:** editing a proposal before approving it (with the sentence that reverses it — "I reject because the folder is wrong, not the task"), a `title` on the task paragraph, a progress bar in `Waiting`, auto-retry of a stranded turn, widening the 360px column alone, a second `page.tsx` for `/chat/[id]`, a `kind` column on `chat_messages`, reading a killed turn's spend out of the usage window, and anything that scrolls or switches tabs on a poll. Unverified: **whether the ten-minute wait actually bothers this operator**, which is the whole case for the live feed and one sentence to settle; **how many proposals ever pile up here** — `chat_proposals` could not be read, both databases in this image being outside the worktree's sandbox, so C5/C6 and most of Option G rest on a seeded 26; whether a killed turn's unrecorded spend (`finishTurn` takes cost from the CLI's final JSON, which a signalled child never prints) is material; and everything about a real turn — no `claude` process was spawned, the workflow proposal card was never rendered, and nothing was heard by a screen reader. Nothing under `src/` changed; `npm run typecheck` exit 0, `npm test` 2085 passing |
| [OrchestratorChatQuality](OrchestratorChatQuality/README.md) | What makes a chat with the orchestrator good, and what makes it bad? | Open; one recommendation, and it is **two prose changes totalling nine lines** — name `ask_operator` in the prompt, and say in `promptOverride`'s description what `save_template.prompt`'s already says. Built on **152 real operator turns across 98 deduplicated conversations** (2026-08-11 to 2026-09-02, 450 `propose_run` calls), found under `/home/node/.claude/projects/-workspace/` and identified by `mcp__uf__*` `tool_use` blocks rather than text mention; `/data` was **not** read, so no proposal here is known to have been approved, rejected or started. **Two of the brief's premises are refuted by the corpus:** it does not propose vaguely (median `task` 4,946 chars, 96.9% naming a concrete file, 1.3% under 500 chars) and it does look before it proposes (89.6% of proposing turns ran Read/Grep/Glob/Bash, 957 `Bash` calls). What replaces them is that **the orchestrator has never once decided on its own to ask a question** — `ask_operator` was called 3 times in 152 turns and all three followed an operator message naming the feature, with at most 1 of 147 replies containing a prose question instead. The mechanism is countable: the CLI defers MCP schemas, 159 of 160 `ToolSearch` calls are explicit `select:mcp__uf__…` fetches, `ask_operator` is named in 3 of them, and it was **fetched in exactly the 3 conversations it was called in** — so the 1,100 characters of asking judgement at `route.ts:302-321`, which its own comment calls "the whole of what the model is told about asking", went unread in 95 of 98 conversations. The one defect with a receipt is a single 23,626-token message that wrote six proposals carrying a 6,921–8,381-char `promptOverride` and **no `task`**, was refused six times, and recovered by re-issuing all six with one identical override (SHA-1 `06a95436`). Also found: `on-finish` + `continueBranch` on 7 of 31 `on-finish` edges, one of them a docs-only run told to record that other work shipped while starting on that run's branch either way; `continueBranch` set on 149 proposals of which only 30 name a template, asserting an isolation guard the model cannot set; and the duplicate check running backwards (`list_proposals` on 61.9% of first proposing turns and 21.4% of later ones). **Refused by name:** capping proposals per message, refusing the edge pair server-side, putting `ask_operator` on the child's `--allowedTools`, moving the asking judgement into the prompt, weakening "prefer proposing", and tightening "Be brief" — whose median 2,188-char reply is what four other required sentences produce. Unverified: **whether the operator wants to be asked more**, one sentence that turns the top recommendation from the fix into a prerequisite; whether any proposal was approved, any work proposed twice, or any `on-finish` dependent ever opened on a crashed branch, all of which need the unreadable database; and whether the chat panel already shows approval order, a thirty-second check nobody made. Nothing under `src/` changed; `npm run typecheck` exit 0 |
| [CustomStacks](CustomStacks/README.md) | How does an operator install a new tool into UsageFoundry without modifying the published container, in a unit other people can reuse for their own tools? | **Decided, reopened as a build specification on 2026-09-12, and the design, the surface and the build order are all written**: the one directory here that is not a survey. **The mechanism, decided against `baf051d` in [01a-mechanism.md](CustomStacks/01a-mechanism.md), [01b-stack-format.md](CustomStacks/01b-stack-format.md), [01c-reach-and-permission.md](CustomStacks/01c-reach-and-permission.md) and [01d-boundaries.md](CustomStacks/01d-boundaries.md):** a stack is a directory holding one `stack.json`, kept in `./stacks/` on the operator's own machine and reaching the container through a read-only bind mount the compose file gains **once**; at boot, before the server starts, an applier installs what each stack declares into the named volume `usagefoundry-stacks` at `/var/lib/uf-stacks`, links the binaries it names into one root-owned `bin` directory the `Dockerfile` has already put on `PATH`, and writes a receipt. Adding the twelfth tool is a directory copied in and a restart, touching no file the image contains, which is R1; the declaration is on the host, so `docker compose up --build` leaves it alone and `docker compose down -v` is answered by reinstalling from it, the volume being a cache and the declaration the durable thing. Because a work cycle's `acceptEdits` may refuse a binary the CLI has never seen and **nobody has measured whether it does**, a stack declares the argv prefixes it grants and the applier projects them onto `--allowedTools` beside `ISOLATED_GIT_TOOLS` (`src/lib/cycleInvocation.ts:633`): the design that works under the worse answer, and one deletable list under the better one, with the exact probe and its four outcomes at `01c-` §4. **Three rulings the design was asked for and made:** `05-`'s derived image is **out**, because build-time layering means an operator on `docker compose pull` cannot add a tool at all, failing R1's own stated test; `04-`'s reconcile question is answered, the applier reconciling in both directions and removing only paths its own receipts record; and identity is the directory name, install-wide, because `14-` §7 found every per-run door closed by name. **The migration question is answered rather than assumed:** all twelve `Dockerfile` commits since 2026-08-25 **stay**, under the rule that a tool `src/` shells out to belongs in the image and a tool only an agent invokes belongs in a stack. `codex` is referenced by 15 files under `src/`, `winnow` by 12, `playwright` by `cycleInvocation.ts` and `smoke-pages.mjs`, and **`jq` by none**, which makes it the one misfiled tool and it stays anyway, because moving it would make `scripts/file-health-check-issues.sh` depend on an operator's directory. Two facts the design rests on were measured in the container rather than read: `command -v unzip` returns nothing, so the operator's own example needs `python3 -m zipfile`, which is stdlib beside the `python3` at `Dockerfile:130`; and that extractor drops a `0755` file to `0644`, which is a `Permission denied` at the far end of a green install if the applier does not `chmod`. **The operator's surface, the read-back, the third-party story, the phases and a full validation pass were written on 2026-09-12 against `fd07353`** in [01e-operator-surface.md](CustomStacks/01e-operator-surface.md), [01f-read-back.md](CustomStacks/01f-read-back.md), [01g-third-party.md](CustomStacks/01g-third-party.md), [01h-acceptance.md](CustomStacks/01h-acceptance.md), [21-implementation-sketch.md](CustomStacks/21-implementation-sketch.md) and [22-validation.md](CustomStacks/22-validation.md). **The surface is not a pane** - a `Tools` section on Settings with stacks as one of three sources, plus `/settings/stacks/<name>` for one receipt, on the eleventh-pane ban's own replacement sentence (`docs/agent/ui-density-audit.md:162`, which still calls `panes.ts` ten rows where `grep -c "href:"` returns **eleven**) - and **nothing in the app installs anything**, because an install endpoint is remote code execution behind this app's own authentication, which `src/lib/config.ts:494-508` already refuses a smaller version of. **R5 reads four layers from four places** - declared, applied, reachable, observed - with the rule that a layer may never be inferred from the one above it, which is what makes a hand-installed tool, a shadowed binary and a receipt that has stopped being true all visible rather than papered over by a confident receipt. **A failed install is written in four places**, because the boot log that carries it is destroyed by the next restart and the restart is when an operator comes looking. **The build order inverts expectation: phase 1 is the read-back**, over the two `UF_*` tool lists that exist today and are read by nothing in `src/`, touching no `Dockerfile`, no compose file, no entrypoint and no volume - worth having on an install that never adds a stack, and shipped before the installer because building that first ships the expensive half first. **The validation pass found seven things and fixed them in place**, two of which were design defects rather than references: `01a-` §7's reconcile would never have retried a failed stack, which quietly broke the claim that the status reading de-latches on a boot; and the schema could neither name a publisher's architecture spelling (`dpkg --print-architecture` says `arm64` where `uname -m` says `aarch64`, measured) nor pin a digest per architecture - both found by writing a **second** worked example, shellcheck and shfmt in one stack, whose publishers ship no checksum manifest and spell architectures the other way. Two findings against the tree are recorded and not fixed: `CLAUDE.md`'s pointer to the `globalThis` shape trap names `orchestrator.ts:373` where the trap is at `:10870-10873`, and the pane-count disagreement above. **The five criteria are checked one by one and R3 is answered *not met*** - its third link has never been run, and reading "designed around" as "met" is the substitution `01h-` exists to refuse; R4a and R5 are *met in design* and cannot be met in fact here. **Still unmeasured and unasked:** R3's third link, every persistence claim (no Docker here, and `grep` over `docs/verification.md` still returns zero lines about any tool volume), and **the single fact that would most change the design**, which is whether the operator can write a directory beside `docker-compose.yml`, since the whole carrier is a host bind mount and an install whose compose file belongs to somebody else has no door at all. `22-validation.md` §5 has the Docker commands in the order they buy the most and §6 is the reasoned-versus-observed accounting for the whole directory. What follows is the specification as the reopening run left it. The operator's words are that it is not about whether we do it, it is about *how*: tools must reach UsageFoundry without the published container being modified, and the mechanism must be modular and easy for other people to use for their own tools. The survey that closed here on 2026-08-25 recommended building almost nothing (one probe, a read-back card, documentation) and **that framing is void**; no file in the directory may re-argue whether. [01-constraints.md](CustomStacks/01-constraints.md) is now **five acceptance criteria with a met/not-met test each**: **R1** adding a tool edits no file inside the published image - *the central one, and a design that fails it is out of the running* - **R2** a stack is a self-contained declarative unit a third party can author, copy and share, **R3** an installed tool reaches every run, every sandboxed run and every kind of agent child, **R4** it survives `docker compose up --build` with `down -v` answered **separately** rather than folded in, and **R5** the app can report what is installed and whether the install succeeded. Beside them, ten constraints the tree imposes with a citation each (the volume-masking trap at `Dockerfile:303-309`, `childEnv`'s strip list at `src/lib/orchestrator.ts:5698-5716`, the uid split, `/data` 0700, denied namespaces, tool state versus the binary, four `CLAUDE.md` invariants that fail silently, the four-files rule for a new `UF_` variable, the left menu's nine digits against **eleven** rows, and that nothing here turns a sandbox on) and two assumptions each naming what changes if it is wrong. **The evidence is measured rather than argued**: `git log --since=2026-08-25T00:00:00 -- Dockerfile` returns **12 commits in the three weeks after this directory closed saying the problem was mostly solved** - eleven distinct changes, ten of them about a tool (`jq`, a Codex CLI pre-install, a Playwright install repair, six version pins) - and, stronger than the count, the Dockerfile makes R1's own argument **three times, for three different tools, in three writers' words**: *"In the image rather than installed by hand … what the next upgrade hands an agent is `codex: command not found` inside a tool call — which no part of the run loop reads"* (`Dockerfile:521-527`). Nobody chose to edit the published image; each of them wrote down that there was no other way. Seventeen option files are **dispositioned rather than deleted** so the design run can read what it supersedes: **six dead** - `03-`, `06-`, `09-`, `10-`, `13-` and `17-`, being the two build-nothing options, a third in a precondition's costume, and the three whose unit is a typed command rather than a shareable artifact - five superseded, six input, with `18-`'s committed repository manifest the strongest survivor because it is *already* the artifact R2 describes and was rejected only on cost, and `05-`'s `Dockerfile.stack` the one to rule on first because it layers on the published image at **build** time where the assumption says boot or run. `19-`, `20-` and `21-` are superseded whole. **Unverified:** R3's third link, whether a work cycle at `acceptEdits` may invoke an arbitrary installed binary, which eleven of the twelve rows the old comparison scored score 0-3 on and which **one work cycle settles**; every persistence claim, because Docker is unavailable in this container and `grep -n "UF_PY_TOOLS\|UF_GH_EXTENSIONS\|usagefoundry-pytools\|gocache" docs/verification.md` now returns **zero** lines where it returned one when the survey closed, so no tool volume has ever been observed surviving a rebuild; and the three one-sentence questions four runs of this directory named as decisive and **none of them asked** - does the operator have host access to the container, what are the five commands they expect to type, and is it one toolchain or four. Citations in `README.md`, `00-` and `01-` were re-verified against `6c5af5f` and every `CLAUDE.md:` citation in the directory was repointed at `docs/agent/`; the rest are known stale (`childEnv` moved from `:6306` to `:5698`, the `acceptEdits` default from `settings.ts:730` to `:940`, the seven-refusals measurement out of `orchestrator.ts` into `cycleInvocation.ts:605-614`) and a later run re-validates them |
